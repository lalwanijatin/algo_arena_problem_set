import java.util.*;
import java.io.*;
import java.nio.file.*;

public class Main{

public static void main(String... args){
		List<Integer> arr = new ArrayList<>();
		try(FileInputStream fis = new FileInputStream("/dev/problems/Calculate Sum/input/##INPUT_FILE_NAME##.txt");
			InputStreamReader isr = new InputStreamReader(fis);
			BufferedReader bufferedReader = new BufferedReader(isr);){
				String line = bufferedReader.readLine(); //Skipping the size of the list
				line = bufferedReader.readLine().trim();
				String[] parts = line.split(" ");
				for(String part : parts){
					arr.add(Integer.parseInt(part));
				}
		}catch(IOException e){e.printStackTrace();}

		System.out.println(calculateSum(arr));

}
		##USER_CODE_GOES_HERE##

}